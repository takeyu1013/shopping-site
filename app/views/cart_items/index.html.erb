<ul class="cart_items">
  <% @cart_items.each do | cart_item | %>
  <li>
    商品名: <%= cart_item.item.name %>
    数量：<%= cart_item.quantity %>
    <%= cart_item.user_id %>
  </li>
  <% end %>
</ul>
<%= button_to "購入する" %>>
